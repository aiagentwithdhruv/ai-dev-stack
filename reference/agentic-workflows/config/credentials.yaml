# Maps each tool to its required environment variables.
# The agent checks this before running a tool to confirm creds are available.
# Update this whenever you add a new tool.

tools: {}
  # Example entries:
  # research_web:
  #   requires: [TAVILY_API_KEY]
  #   optional: [EXA_API_KEY]
  # send_email:
  #   requires: [SMTP_HOST, SMTP_USER, SMTP_PASSWORD]
  # scrape_website:
  #   requires: []
  #   notes: "No API key needed — uses requests/beautifulsoup"
